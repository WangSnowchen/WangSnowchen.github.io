---
title: 文章标题
description: 一句话说清这篇讲什么。会出现在首页列表和搜索结果里。
date: 2026-09-15
tags: ['Gaffer', 'OSL']
# cover: /images/封面图.png  ← 要用封面就取消这行注释，图放进 public/images/
---

开头一两段交代背景：遇到了什么问题，为什么值得写下来。

## 问题是什么

描述现象，可以贴报错、贴截图。

## 思路

解释你的推导过程。

## 实现

核心代码：

```python
# 代码块会自动做语法高亮，明暗主题各一套
```

## 效果对比

改前改后值得对照的话，截图放进 `public/images/`，
再用 `compare` 代码块引用——拖中间的滑块看差异。写法见 WRITING.md。

## 踩过的坑

> 引用块适合放这类提醒。

- 要点一
- 要点二

## 小结

收个尾。

<!--
这个文件是给你复制用的，放在 templates/ 下面，不会被当成文章发布。

发新文章：
  1. cp templates/post.md src/content/posts/文件名.md
     文件名会变成网址：foo-bar.md → /post/foo-bar/，建议用英文加短横线
  2. 改 frontmatter，写正文
  3. 图片放进 public/images/，正文里用 /images/文件名 引用
  4. 提交，开 PR —— 自动检查会告诉你哪里有问题

上面这些区块不是必须的，按你的文章增删。

为什么这里没有图片和对比图的完整示例：内容检查是逐行扫的，连注释里的图片语法
都会当成真引用去校验文件是否存在。模板保持"复制出来就能过检查"，示例集中在
根目录的 WRITING.md 里。
-->
